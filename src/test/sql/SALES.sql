
SELECT *
FROM SALES
LIMIT 10;



-- -------------------------------------------------------------------------------------------------------- SALES_DATE
-- -------------------------------------------------------------------------------------------------------- SALES_PERSON
-- -------------------------------------------------------------------------------------------------------- REGION
-- -------------------------------------------------------------------------------------------------------- SALES


SELECT SALES_DATE, SALES_PERSON, REGION, COUNT(1)
FROM SALES
GROUP BY SALES_DATE, SALES_PERSON, REGION
HAVING COUNT(1) > 2
;