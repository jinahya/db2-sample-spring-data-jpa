#!/bin/sh
## Uncheck the [Use Rosetta for x86_64/amd64 emulation on Apple Silicon] option in Docker Desktop
## Uncheck the [Use Rosetta for x86_64/amd64 emulation on Apple Silicon] option in Docker Desktop
## Uncheck the [Use Rosetta for x86_64/amd64 emulation on Apple Silicon] option in Docker Desktop
## Uncheck the [Use Rosetta for x86_64/amd64 emulation on Apple Silicon] option in Docker Desktop
export DOCKER_DEFAULT_PLATFORM=linux/amd64
docker-compose up -d
